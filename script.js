let myLibrary = [hungerGames = new Book('The Hunger Games', 'Suzanne Collins', 374, false)];

function Book(title, author, pageCount, isRead) {
  // the constructor...
  this.title = title
  this.author = author
  this.pageCount = pageCount
  this.isRead = isRead
}

function addBookToLibrary() {
  // do stuff here
}